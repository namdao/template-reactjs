/*
 * Dashboard alias name
 *
 * This contains all dashboard template
 */
import { defineMessages } from 'react-intl';

export const scope = 'translate.constant';

export default defineMessages({
  vi: {
    id: `${scope}.vi`,
  },
  en: {
    id: `${scope}.en`,
  },
});
